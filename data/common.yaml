---
profile_email::canonical_aliases: |
  # This file is managed by Puppet.
  root /dev/null
profile_email::virtual_aliases: |
  # This file is managed by Puppet.
  root /dev/null
profile_email::mydomain: 'local'
profile_email::relayhost: 'localhost'
